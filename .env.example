# Local development defaults. Copy to .env (git-ignored) and adjust as needed.

# Base URL for the backend API. Defaults to http://localhost:8001 if unset.
# Example for local backend:
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8001

# Example for staging or production:
# NEXT_PUBLIC_API_BASE_URL=https://api.healtharchive.ca
