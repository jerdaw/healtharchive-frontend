# eslint-plugin-react@7.37.5 declares eslint@^9.7 as a peer dep but works fine
# with eslint 10 after the patch in patches/eslint-plugin-react+7.37.5.patch.
# Remove this once eslint-plugin-react ships eslint 10 support upstream.
legacy-peer-deps=true
